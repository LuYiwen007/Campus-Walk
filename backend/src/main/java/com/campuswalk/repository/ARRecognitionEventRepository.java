package com.campuswalk.repository;

import com.campuswalk.entity.ARRecognitionEvent;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ARRecognitionEventRepository extends JpaRepository<ARRecognitionEvent, Long> {
}
